# 项目界面约定

- 用户要求所有界面改动同时适配浅色、深色及跟随系统模式。
- 新增颜色与交互状态优先使用 `sanmu/theme.py` 的语义色板；自绘控件跟随当前应用主题。
- 修改后检查受影响页面在两种主题下的文字、背景、悬停、选中、禁用与弹窗状态。
- 调整布局时检查窄窗口、分栏拖拽和滚动，保持点餐与结算操作可见；切换主题不清空正在编辑的内容。
- 热敏小票保持白底黑字，用户 Logo 保留原图含义，打印配色与软件主题分开处理。
